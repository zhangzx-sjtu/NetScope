本作品源代码为python，主要应用的库为Scapy和PyQt4。
操作系统为ubuntu，经测试在ubuntu 14.04 和 ubuntu 18.04下均可以成功编译运行。

icon文件夹包含作品的手绘图标

编译环境为以下两种均可：
（1）PyCharm开发环境下编译运行。
（2)  在linux终端窗口中，定位到main.py所在的目录下，输入命令python main.py也可以编译运行。